name: refazynist
description: a Refreshable, Lazy, Animated List
version: 0.0.3
homepage: https://github.com/themt-co/refazynist

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true